createMessage:
  handler: ${self:custom.functionsDirectory}/message/createMessage.handler
  events:
    - httpApi:
        path: /message
        method: post

findAllMessage:
  handler: ${self:custom.functionsDirectory}/message/findAllMessage.handler
  events:
    - httpApi:
        path: /message
        method: get